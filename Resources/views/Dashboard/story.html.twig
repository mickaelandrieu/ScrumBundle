{% set story = story[0] %}
<div class="story ">
    <a class="floatL" href="{{ path('scrum_story_show',{'id_project':id_project,'id':story.id }) }}">{{ story.name }}</a>
    <div class="floatR" >
    
    {% include 'NicoBScrumBundle:Dashboard:status.html.twig' %}    
        
    {{ story.priority.name }}/{{ story.difficulty.name }}

    </div>
    <div class="clear"></div>

</div>