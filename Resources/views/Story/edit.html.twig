{% extends 'NicoBScrumBundle::layout.html.twig' %}


{% block content %}

<h1>Story edit</h1>

<form action="{{ path('scrum_story_edit', { 'id': entity.id,'id_project':id_project }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button class="btn btn-primary" type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a class="btn btn-primary" href="{{ path('scrum_story',{'id_project':id_project}) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}