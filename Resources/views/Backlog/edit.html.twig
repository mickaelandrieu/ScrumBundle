{% extends 'NicoBScrumBundle::layout.html.twig' %}


{% block content %}

<h1>Backlog edit</h1>

<form action="{{ path('backlog_edit', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button class="btn btn-primary" type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a class="btn btn-primary" href="{{ path('backlog') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}