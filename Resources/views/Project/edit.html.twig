{% extends 'NicoBScrumBundle::layout.html.twig' %}


{% block content %}

<h1>Project edit</h1>

<form action="{{ path('project_edit', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button class="btn btn-primary" type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a class="btn btn-primary" href="{{ path('project') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}